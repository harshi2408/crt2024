#include<stdio.h>
void main()
{
int num,i,count,m;
scanf("%d",&m);
for(num=1;num<=m;num++){
for(i=2,count=0;i*i<=num;i++){
if(num%i==0)
count++;
}
if(count==0 &&(num!=1))
printf("%d ",num);
}
}
output:
10
2 3 5 7 

